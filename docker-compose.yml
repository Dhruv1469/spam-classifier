services:
  backend:
    build: ./backend
    container_name: spam-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: spam-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend